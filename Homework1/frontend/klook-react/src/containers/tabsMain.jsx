import ShowTabs from '../components/content/tabs';

function TabsContainer({ activeTab, setActiveTab }) {
    return <ShowTabs activeTab={activeTab} setActiveTab={setActiveTab}></ShowTabs>
}

export default TabsContainer
